import { BudgetDirective } from './budget.directive';

describe('BudgetDirective', () => {
  it('should create an instance', () => {
    const directive = new BudgetDirective();
    expect(directive).toBeTruthy();
  });
});
